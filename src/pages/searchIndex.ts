import { graphql } from "gatsby"

export default () => null

// This query errors out if there's no content to index; just commenting it out for now.

// export const query = graphql`
//   query SearchIndexQuery {
//     siteSearchIndex {
//       index
//     }
//   }
// `
